/**
 * UI Component for viewing the Markdown export with YAML frontmatter.
 */
import React from 'react';

interface MarkdownExportViewProps {
  markdownContent: string;
}

export const MarkdownExportView: React.FC<MarkdownExportViewProps> = ({ markdownContent }) => {
  // ...existing code...
  return <div>Markdown Export View</div>;
};
